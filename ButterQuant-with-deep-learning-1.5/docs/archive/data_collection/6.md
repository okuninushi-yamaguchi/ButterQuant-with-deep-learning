# 每日扫描配置文件

# 扫描器配置
scanner:
  # 请求间隔（秒）- 避免API限流
  request_interval: 15
  
  # 批次检查点 - 每N个股票保存一次中间结果
  batch_checkpoint: 10
  
  # 数据目录
  data_dir: "backend/data"
  
  # 超时设置
  timeout: 30

# 定时任务配置
scheduler:
  # 时区
  timezone: "Asia/Tokyo"
  
  # 执行时间 (24小时制)
  run_hour: 18
  run_minute: 0
  
  # 是否启用
  enabled: true

# 数据源配置
data_source:
  # 股票列表文件
  nasdaq100_file: "backend/data/nas100.md"
  sp500_file: "backend/data/sp500.md"
  combined_file: "backend/data/combined_tickers.md"

# 输出配置
output:
  # 结果文件
  rankings_file: "backend/data/rankings_combined.json"
  top20_file: "backend/data/rankings_top20.json"
  
  # 进度文件
  progress_file: "backend/data/scan_progress.txt"
  
  # 日志文件
  log_file: "backend/data/scan_log.txt"
  scheduler_log_file: "backend/data/scheduler.log"

# 日志配置
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# yfinance 配置
yfinance:
  # 是否使用代理
  use_proxy: false
  
  # 代理地址（如果使用）
  proxy: null
  
  # 重试次数
  max_retries: 3
  
  # 重试延迟（秒）
  retry_delay: 5

# API配置
api:
  # 是否启用缓存
  enable_cache: true
  
  # 缓存过期时间（秒）
  cache_ttl: 86400  # 24小时
  
  # 返回结果数量
  default_page_size: 20
  max_page_size: 100